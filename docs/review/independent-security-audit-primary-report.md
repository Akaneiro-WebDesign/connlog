# ConnLog 独立セキュリティ監査 一次結果報告書

作成日：2026年8月7日
最終更新：2026年8月10日
状態：進行中
対象：MVP公開前の独立した一次セキュリティ監査

## 1. この報告書の位置付け

この文書は、独立セキュリティ監査で現時点までに確認できた結果を記録する途中報告である。

未実施の確認項目が残っているため、ConnLog全体の安全性についての最終結論や、MVP公開可否の判定を示すものではない。

監査手順、対象範囲、禁止操作および報告方針は、`docs/review/independent-security-audit-test-spec.md` に従う。

## 2. 対象環境とテストデータ

- 対象環境：許可されたConnLogテスト環境
- 確認日：2026年8月7日、8月8日、8月10日
- 使用アカウント：テストアカウントA、テストアカウントB
- 使用データ：各テストアカウントに紐づくテストデータのみ
- 実ユーザーのデータ、認証情報、Cookie、トークンは確認・記録していない

## 3. 確認済み結果

### IA-PRIM-001：イベント一覧におけるユーザー間の表示分離

- 確認項目：アカウントAのイベントが、アカウントBのイベント一覧に表示されないこと
- 対象画面：`/events`
- 実施した操作：
  1. アカウントAで、A専用だと識別できるテストイベントが1件以上存在することを確認した
  2. アカウントBで`/events`を開いた
  3. Bのイベント一覧にA専用イベントが表示されないことを確認した
- 期待結果：Bのイベント一覧にAのイベントが表示されない
- 実際の結果：BにAのイベントは表示されなかった
- 判定：画面上のイベント一覧について、A・B間の表示分離を確認
- 根拠：2026年8月7日のテストアカウントA・Bによる手動確認
- 制約・未確認事項：
  - APIリクエストを直接改変した取得試験は未実施

### IA-PRIM-002：ダッシュボードにおけるユーザー間の表示分離

- 確認項目：アカウントAのイベント、タグ、メモに由来する情報が、アカウントBのダッシュボードに表示されないこと
- 対象画面：`/dashboard`
- 実施した操作：
  1. アカウントAに専用イベントが存在する状態で、アカウントBの`/dashboard`を開いた
  2. Aのイベント、タグ、メモに由来する表示がBのダッシュボードに混在しないことを確認した
- 期待結果：BのダッシュボードにA由来のデータが表示されない
- 実際の結果：BのダッシュボードにAのデータは見当たらなかった
- 判定：画面上のダッシュボードについて、A・B間の表示分離を確認
- 根拠：2026年8月7日のテストアカウントA・Bによる手動確認
- 制約・未確認事項：
  - 集計APIのリクエスト改変試験は未実施
  - 表示されないことの確認であり、全データ取得経路を網羅した結論ではない

### IA-PRIM-003：タグ・メモ更新およびイベント削除におけるユーザー間認可

- 確認項目：アカウントBが、アカウントAのタグ・メモを変更できず、イベント本体を削除できないこと
- 対象API：`PUT /api/events/update`、`DELETE /api/events/delete`
- 使用データ：Aの監査専用イベント（内部ID：`125`、外部イベントID：`401704`）
- 実施した操作：
  1. BでAの外部イベントIDを指定して更新APIを呼び出した
  2. Aでイベントを確認し、A側のタグ・メモが変更されていないことを確認した
  3. Bで作成された監査用のタグ・メモを、関連データ削除APIで削除した
  4. BでAのイベント内部IDを指定して削除APIを呼び出した
- 期待結果：BはAのタグ・メモおよびイベント本体を更新・削除できない
- 実際の結果：
  - 更新APIは`200`を返したが、A側のタグ・メモは変更されなかった
  - B側でのみ監査用タグ・メモが一時的に作成され、B自身の操作で削除できた
  - 削除APIは`404`と「イベントが見つからないか、削除権限がありません」を返し、Aのイベント本体は削除されなかった
- 判定：BによるAデータの更新・削除は確認されなかった
- 根拠：
  - 2026年8月8日のテストアカウントA・Bによる直接API呼び出しと、A画面での変更有無確認
  - `events`・`tags`・`notes`の現行RLS Policyが、各ユーザーの`user_id`と`auth.uid()`の一致を条件としていること
- 補足・改善候補：
  - 更新APIは対象イベントの所有確認を行わず、Bが所有しない外部イベントIDを指定しても`200`を返す
  - RLSによりAのデータは変更されないが、B自身のタグ・メモがイベント本体なしで作成され得る
  - 越境更新の脆弱性ではない。一方で、APIの応答整合性と孤立した関連データの防止のため、MVP後の低優先度改善候補とする

### IA-PRIM-004：RLS Policyと実地テスト結果の照合

- 確認項目：`events`・`tags`・`notes`の現行RLS Policyが、テストアカウント間のデータ分離と整合すること
- 実施した操作：Supabase SQL Editorで`pg_policies`を参照し、各テーブルのPolicy条件を確認した
- 実際の結果：
  - `events`はSELECT・INSERT・UPDATE・DELETEの各操作で、`auth.uid() = user_id`を条件としている
  - `tags`はSELECT・INSERT・UPDATE・DELETEの各操作で、`auth.uid() = user_id`を条件としている
  - `notes`も本人の`user_id`に限定するPolicyが設定されている
- 判定：今回の表示分離および更新・削除拒否の実地テスト結果と、RLS Policyの方向性は整合している
- 制約・未確認事項：
  - 全API・全クエリ経路を網羅する検証ではない
  - `notes`には同じ目的のPolicyが複数存在するため、MVP後に不要Policyの整理を検討する

### IA-PRIM-005：publicスキーマのDB関数・実行権限と未使用関数の整理

- 確認項目：`public`スキーマのDB関数、`SECURITY DEFINER`設定、実行権限、トリガーおよび依存関係
- 実施した操作：
  1. `pg_proc`・`pg_namespace`を参照し、`public`スキーマの関数一覧と実行権限を確認した
  2. `pg_trigger`を参照し、各関数がトリガーから使用されているか確認した
  3. `pg_depend`とリポジトリ内検索により、未使用関数の依存関係と参照を確認した
  4. 未使用と確認できた`public.set_user_id()`を、`RESTRICT`付きのSQLで削除した
  5. 削除後に関数一覧を再取得し、必要な関数だけが残っていることを確認した
- 削除前の確認結果：
  - `handle_new_user()`は`SECURITY DEFINER`として定義され、`auth.users`への新規ユーザー追加時に実行される`on_auth_user_created`トリガーから使用されていた
  - `handle_new_user()`の明示的な実行権限は`postgres`と`service_role`に限定され、`anon`・`authenticated`・`PUBLIC`には付与されていなかった
  - `set_user_id()`は`SECURITY DEFINER`ではなかったが、`PUBLIC`・`anon`・`authenticated`を含む広い実行権限が残っていた
  - `set_user_id()`を使用するトリガー、依存オブジェクトおよびリポジトリ内の参照は確認されなかった
- 対応内容：
  - `docs/db/2026-08-10-remove-unused-set-user-id-function.sql`を作成した
  - `DROP FUNCTION IF EXISTS public.set_user_id() RESTRICT;`を実行した
  - `CASCADE`は使用せず、想定外の依存関係があれば削除に失敗する形とした
- 対応後の結果：
  - `to_regprocedure('public.set_user_id()')`が`NULL`を返すことを確認した
  - `public`スキーマには、使用中の`handle_new_user()`だけが残っていることを確認した
- 判定：
  - `set_user_id()`は権限昇格を行う関数ではなく、使用中の脆弱な経路も確認されなかった
  - 不要な関数と実行権限を削除し、DB上の不要な公開面を縮小した
  - `handle_new_user()`は必要なトリガー関数として残し、直接実行できるロールが制限されていることを確認した
- 制約・未確認事項：
  - 今回は関数の配置、権限、トリガーおよび依存関係の確認が中心であり、DBスキーマ全体の再現性確認ではない
  - 現在のDB変更SQLは`docs/db`に保存されているが、`supabase/migrations`による自動適用可能なmigration構成にはなっていない

## 4. 現時点のまとめ

テストアカウントA・Bを用いた通常画面の確認では、次の表示分離を確認した。

- Bのイベント一覧にAのテストイベントは表示されない
- BのダッシュボードにA由来の情報は見当たらない
- BによるAのイベント本体の削除は`404`で拒否された
- BがAの外部イベントIDを指定しても、A側のタグ・メモは変更されなかった
- `public`スキーマのDB関数と実行権限を確認した
- 未使用だった`set_user_id()`を、依存関係がないことを確認したうえで削除した
- 使用中の`handle_new_user()`だけが、制限された実行権限で残っていることを確認した

ただし、これは通常画面操作、限定的な直接API呼び出し、RLS Policy参照、およびDB関数・権限の限定的な確認結果である。全API・全データ取得経路、使用中DB関数の定義内容、DBスキーマ全体のmigration管理状況などの確認は継続して実施する。

## 5. 継続して確認する項目

- BがAのタグ・メモを直接取得、更新、削除できないこと
- API Route Handlerにおける認証・認可
- アカウント削除時の本人確認と対象データの絞り込み
- service role keyの使用箇所とサーバー側限定の確認
- 使用中DB関数の定義内容と、DBスキーマ全体のmigration管理・再現性の確認
- 依存関係とセキュリティヘッダーの独立確認
- 更新APIで対象イベントの所有確認を行うべきかの判断
