# ConnLog 少人数テストユーザー招待前 最終QA記録

確認日：2026年8月28日

対象環境：Production

対象コミット：`f0ddb83`

## 1. この文書の目的

この文書は、ConnLogへ少人数のテストユーザーを招待する前に、Production環境の主要機能と主要導線を最終確認し、その結果と公開可否の判断を記録するものである。

これまでに実施したコード監査、RLS・API確認、テストアカウントA・Bによるデータ分離確認、Claude Codeによる独立一次監査、Supabase・Vercel設定確認を置き換えるものではない。

今回は、これらの確認と修正を反映した最新版のProduction環境で、一般利用者が実際に操作する主要導線が正常に動作することを確認する。

## 2. 確認方針

最終QAは次の方針で行う。

- Production環境を対象とする
- 既存のテストアカウントA・Bを使用する
- メールアドレス、パスワード、APIキーなどの秘密情報は記録しない
- イベントの追加・更新・削除にはテストデータだけを使用する
- 他ユーザーのデータが表示・更新・削除されないことを確認する
- アカウント削除は、削除してよい専用アカウントがある場合だけ実施する
- QA中に不具合を発見した場合は、このブランチへ修正を混ぜず、別の修正ブランチとPRで対応する
- テストユーザーの実際の招待は、最終QAと公開可否判断が完了するまで行わない

## 3. 判定基準

次のいずれかが確認された場合は、テストユーザーの招待をいったん見送る。

- ログインや主要画面への移動ができない
- イベントの検索・保存・更新・削除が正常に動作しない
- 他ユーザーのデータを閲覧・更新・削除できる
- パスワード変更などの重要操作で本人確認が機能しない
- 利用を継続できない重大な画面崩れやエラーが発生する
- 秘密情報の露出や認証回避につながる問題が見つかる

表示上の軽微な違和感など、主要機能と安全性を損なわない問題は、内容と対応方針を記録したうえで公開可否を判断する。

## 4. 最終QA結果

各項目は、Production環境で実際に操作して確認する。

- `[x]`：想定どおりに動作
- `[ ]`：未確認
- 問題や補足がある場合は、該当項目の直下へ記録する

### 4-1. 未ログイン状態と公開ページ

- [x] Production URLへHTTPSでアクセスできる
- [x] トップページが重大な表示崩れなく表示される
- [x] `/login`にログインフォームが表示される
- [x] `/signup`に一般登録フォームが表示されない
- [x] `/signup`に「ConnLogは現在、招待制で公開しています」と表示される
- [x] `/privacy-policy`を閲覧できる
- [x] `/terms`を閲覧できる
- [x] 未ログイン状態で保護対象ページへアクセスすると、ログイン画面へ移動する
- [x] 公開ページの画面上に、利用者のメールアドレス、秘密のAPIキー、環境変数の値、エラー詳細などが表示されない

### 4-2. ログインとログアウト

テストアカウントAを使用して確認する。

- [x] 誤ったパスワードではログインできない
- [x] 誤ったパスワードを入力した場合にエラーが表示される
- [x] 正しいメールアドレスとパスワードでログインできる
- [x] ログイン後に認証が必要な画面へ移動できる
- [x] ページを再読み込みしてもログイン状態が維持される
- [x] ログアウトできる
- [x] ログアウト後は認証が必要な画面を閲覧できない
- [x] ログアウト後にブラウザの戻る操作をしても、認証が必要な操作を継続できない

### 4-3. イベント検索と保存

テストアカウントAを使用し、保存してもよいconnpassイベントで確認する。

- [x] `/search`を正常に表示できる
- [x] 正しいconnpassイベントURLでイベントを検索できる
- [x] 検索結果にイベント名、開催日時、主催者またはグループなどの基本情報が表示される
- [x] 不正な形式のURLを入力した場合、保存されず利用者向けのエラーが表示される
- [x] 検索結果からイベントを保存できる
- [x] 保存完了が画面上で分かる
- [x] 保存したイベントが`/events`に表示される
- [x] 同じイベントを再度保存しても、イベント履歴に重複して登録されない

### 4-4. イベント履歴とタグ・メモの更新

テストアカウントAを使用し、4-3で保存したイベントで確認する。

- [x] `/events`を重大な表示崩れなく表示できる
- [x] 保存したイベントのイベント名、開催日時、主催者またはグループなどの基本情報が表示される
- [x] 保存したイベントの編集画面を開ける
- [x] テスト用のタグを追加できる
- [x] テスト用のメモを入力して保存できる
- [x] 保存後の画面に追加したタグとメモが反映される
- [x] ページを再読み込みしてもタグとメモが維持される
- [x] タグまたはメモを変更し、再度保存できる
- [x] 再読み込み後も変更後の内容が維持される

### 4-5. ダッシュボードとスキル画面

テストアカウントAを使用し、4-4で追加したテスト用タグを残した状態で確認する。

- [x] `/dashboard`を重大な表示崩れやエラーなく表示できる
- [x] イベント件数などの集計情報が表示される
- [x] タグに関する集計へテスト用タグが反映される
- [x] 週ごとの参加数を表すグラフまたは集計を表示できる
- [x] `/skills`を重大な表示崩れやエラーなく表示できる
- [x] `/skills`にテスト用タグが表示される
- [x] ページを再読み込みしても集計結果とテスト用タグが表示される

### 4-6. プロフィール情報の更新

テストアカウントAを使用して確認する。

補足（対応・再確認済み）：Chromeのシークレットウィンドウでログイン・ログアウト後、認証必須ページのデータ読み込みに失敗することが複数回あった。VercelのProductionログでは、`GET /api/profile`と`POST /api/dashboard-data`が500を返し、Supabaseから`PGRST303`かつ`JWT issued at future`が返されていた。

対策として、この条件に一致した場合は1秒後に1回だけ再試行する処理を追加した。

2026年8月31日にProduction環境で、ログイン直後の`/profile`の初回表示と再読み込み、および`/events`・`/dashboard`・`/skills`の表示を再確認し、500エラーや意図しないログアウトが発生しないことを確認した。

- [x] `/profile`を重大な表示崩れやエラーなく表示できる
- [x] 現在の表示名と自己紹介が表示される
- [x] 表示名と自己紹介をテスト用の内容へ変更できる
- [x] 保存完了が画面上で分かる
- [x] ページを再読み込みしても変更後の内容が維持される
- [x] 表示名と自己紹介を確認前の内容へ戻せる
- [x] 再読み込み後も元の内容が維持される

### 4-7. Productionデプロイとスマートフォン表示

2026年8月31日、PR #110とPR #111のマージ後にProduction環境で確認した。

- [x] Vercelの最新デプロイが`Production`かつ`Ready`になっている
- [x] 最新デプロイが`main`のマージコミット`774c704`に対応している
- [x] Pixel 7aで`/events`を重大な表示崩れなく表示できる
- [x] 長いイベント名、説明、タグが画面幅を押し広げない
- [x] ページ全体に不自然な横スクロールが発生しない
- [x] 操作部分がスマートフォンの画面内に収まる
- [x] Pixel 7aをダークモードにしてもConnLogがライトモードで表示される

### 4-8. パスワード変更

2026年9月1日から9月3日にかけて、テストアカウントAを使用してProduction環境で確認した。

確認中、現在のパスワードが間違っている場合にも、一時的な失敗を示す汎用メッセージが表示されることを確認した。PR #113で、Production環境から返された`current_password_invalid`と互換用の`current_password_mismatch`を個別に処理し、「現在のパスワードが正しくありません。」と表示するよう修正した。マージコミットは`8e8ced8`。

- [x] 8文字未満の新しいパスワードが拒否される
- [x] 新しいパスワードと確認用パスワードが一致しない場合に拒否される
- [x] 間違った現在のパスワードでは変更できない
- [x] 現在のパスワードが間違っていることが画面上で分かる
- [x] 正しい現在のパスワードで一時パスワードへ変更できる
- [x] 変更成功が画面上で分かり、予期しないエラーが発生しない
- [x] ログアウト後、一時パスワードで再ログインできる
- [x] 一時パスワードから元のパスワードへ復元できる
- [x] ログアウト後、元のパスワードで再ログインできる


### 4-9. テストアカウント間のデータ分離

2026年9月4日、テストアカウントA・Bを使用してProduction環境で確認した。

- [x] Aで保存したイベント・タグ・メモがBの`/events`・`/dashboard`・`/skills`に表示されない
- [x] Bで保存した同一connpassイベントに、Aとは異なるタグ・メモを設定できる
- [x] AとBの各画面には、それぞれ自分のタグ・メモ・集計だけが表示される
- [x] Aのテストイベントを削除してもBの同一イベントは削除されない
- [x] 確認後、A・B双方のテストデータを削除した
- [x] 確認中に500エラーや意図しないログアウトが発生しない

### 4-10. スマートフォンの主要画面

2026年9月4日、Pixel 7aを使用してProduction環境で確認した。

- [x] `/search`を重大な表示崩れなく表示・操作できる
- [x] `/dashboard`を重大な表示崩れなく表示・操作できる
- [x] `/skills`を重大な表示崩れなく表示・操作できる
- [x] `/profile`を重大な表示崩れなく表示・操作できる
- [x] 主要画面で不自然な横スクロールや、操作を妨げる表示崩れが発生しない

### 4-11. ConsoleとNetwork

2026年9月4日、Chromeのゲストモードを使用してProduction環境で確認した。

確認中、Vercel Toolbar由来の`feedback.js`がContent Security Policyによってブロックされる赤いエラーが表示された。VercelのProduction Deploymentsに対するToolbar設定を`Off`へ変更し、新しいゲストウィンドウで再確認したところ、赤いエラーが表示されないことを確認した。

- [x] `/search`・`/events`・`/dashboard`・`/skills`・`/profile`で、Consoleに赤いエラーが表示されない
- [x] `profile`と`dashboard-data`のリクエストが200を返す
- [x] 確認した通信結果が200または304で、4xx・5xx・`Failed`が発生しない
- [x] 画面表示、主要操作、API通信に重大な問題が発生しない

補足：Consoleには、`Permissions-Policy`の`attribution-reporting`を認識できないことを示す黄色い警告が残っている。画面表示、主要操作、API通信への影響は確認されていないため、少人数テストユーザーの招待を妨げる問題とは判断しない。
